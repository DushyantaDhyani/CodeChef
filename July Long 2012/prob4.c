#include<stdio.h>
#include<conio.h>
int main()
{
    int N,k,i;    
    scanf("%d",&N);
    scanf("%d",&k);
    long arr[N];
    for(i=0;i<N;i++)
    {
                    scanf("%ld",&arr[i]);
    }
    for()
}
